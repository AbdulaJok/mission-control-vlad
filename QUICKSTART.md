# 🚀 Mission Control — Быстрый Старт

## ⚡ Что это
Визуальный интерфейс для твоего OpenClaw: задачи, память, календарь, агенты. Всё в одном месте.

## 🔐 Шаг 0: Безопасность (КРИТИЧНО)
Ты только что сбросил GitHub Secret. Теперь нужно вставить **новый** в локальный файл.

1. Открой файл: `.env.local` в этой папке
2. Вставь новый секрет вместо `<ВСТАВЬ_НОВЫЙ_СЕКРЕТ_СЮДА>`
3. **Никогда не коммить этот файл в Git!** (он уже в `.gitignore`)

## 🛠️ Шаг 1: Установка зависимостей
```bash
cd /home/clawd/.openclaw/workspace/staging/mission-control
npm install
```

## 🔗 Шаг 2: Подключение Convex
Тебе нужно создать проект в Convex (это бесплатно):

1. Зайди на https://dashboard.convex.dev
2. Нажми **"New Project"** -> Назови `mission-control`
3. Скопируй URL из **Settings** (выглядит как `https://your-project.convex.cloud`)
4. Вставь его в `.env.local` вместо `https://your-project.convex.cloud`

## 🚀 Шаг 3: Запуск
Открой **два терминала**:

**Терминал 1 (Бэкенд):**
```bash
npx convex dev
```
*Это запустит Convex и задеплоит схему базы данных.*

**Терминал 2 (Фронтенд):**
```bash
npm run dev
```
*Это запустит NextJS сервер.*

## 🎉 Шаг 4: Готово
Открой в браузере: **http://localhost:3000**

Ты увидишь:
- 📌 **Tasks** — твои задачи из `memory/tasks.md` (ЕГЭ, физика, математика)
- 🧠 **Memory** — поиск по всем заметкам
- 📅 **Calendar** — события и дедлайны
- 👥 **Team** — список агентов

## 🔄 Синхронизация с файлами
Чтобы импортировать текущие `.md` файлы в базу:
```bash
npm run sync-memory
```

## 📦 Деплой на Vercel (опционально)
Чтобы открыть с телефона:
```bash
npm install -g vercel
vercel --prod
```
*Не забудь добавить переменные окружения в настройках Vercel проекта!*

---
**Проблемы?** Пиши в Telegram. 🦞
